# 验证记录

更新日期：2026-09-11。分支 `codex/match-settings-reference`，仓库尚无提交；继续修改已有任务文件，没有提交、发布或安装到用户 Chrome。

## 自动验证

- `npm test`：34 / 34。覆盖语言规则、设置校验、三种协议、实际本地 HTTP 请求、UTF-8 / JSON 转义跨块、首段在结束前到达、错误方向、流内错误、缺失结束标记、截断、断流超时和 reader 取消、非流式兼容响应，以及思考参数、设置保存、阶段去重、取消和推理文本隔离。
- 后台边界覆盖 Key 隔离、权限和存储等待时取消、旧请求隔离、开关与配置保存竞争、端口断开取消、端口不能读取设置 / 替换 Key / 重复发起请求、移除独立配置跳转消息。
- `npm run check`：生产 JS 语法、MV3 清单资源、本地资源、CSP 与 PNG 尺寸通过。
- `cmp extension/icons/ai-translate.svg /Users/zhengjieshen/Downloads/ai-translate.svg` 通过，图标内容未改动。

## 浏览器交互

使用 CUA 内置 Chromium 浏览器和明确标注的测试 Chrome / AI 适配器。

- `http://127.0.0.1:4173/`：内容脚本浏览器测试 18 / 18。只选中不请求、首段提前显示、原文默认折叠及字数、完成前禁用复制、断流保留片段并标记未完成、独立滚动及固定操作、四角边界和 70% 高度上限、流式保持阅读位置、中文转英语、HTML 纯文本、错误重试入口、关闭和新选区隔离、功能关闭、密码框不触发，以及真实事件驱动的思考阶段、用时和清理。
- 真实 UI 点击验证：原文展开 / 折叠；复制后显示“已复制”；翻译弹框截图中标题、方向、原文、译文、重译和复制均可见。
- `http://127.0.0.1:4173/preview/popup.html`：直接显示五字段配置，不经菜单跳转。验证保存、重新加载后的模型回显、模型必填提示、功能开关、同窗口连接测试成功反馈。测试使用假 Key 和模拟响应。
- 悬浮配置 400×600 和 375×600 视口均无横向溢出；保存按钮底部约 555.5px，连接测试成功提示底部约 584.7px，均在 600px 范围内。
- 翻译弹框在 1280×900 桌面和 375×812 窄屏检查；修复定位使用 `innerWidth` 时右侧可能被滚动条遮挡的问题，改用实际可用的 `clientWidth`，新边界测试通过。
- 页面身份正确、非空白、无错误覆盖层；配置和内容脚本页面控制台无错误或警告。截图已检查。

## 参考一致性

- 参考文件：`selection-translation-design.html` 中 `#selection-translation` 的样式；保留 10px 圆角、紧凑头部及底部操作布局，支持深色。后续按用户要求把默认宽度改为 440px，原文默认折叠显示字数，高度最高为可视窗口的 70%。
- 图标采用用户提供的品牌设计，现按最新要求切换为 `ai-translate-light.svg` 浅色版本，原暗色 `ai-translate.svg` 及 PNG 完整保留。
- 保留既有自动翻译方向规则，头部目标语言显示为标签；未增加参考演示中的手动目标语言选择功能。
- 文本随真实 API 分片追加，并有 180ms 淡入；不会等完整文本收到后再模拟打字。减少动态效果模式不播放动画。

## 证据边界

- 已通过真实本地 HTTP SSE 验证首段先到达与请求取消；浏览器使用实际 UI / content.js，Chrome 与 AI 边界为测试适配器，适配器不进入扩展包。
- 已用用户授权的 DeepSeek 配置完成真实思考开关与阶段验证，未把 Key 写入源码、测试文件或报告。未向用户 Chrome 安装扩展，原生 MV3 加载、工具栏弹窗及 Chrome 授权需重新加载插件后确认。
- 复制按钮返回“已复制”，未跨应用核实粘贴。
- 服务商若忽略流式参数，仍能解析正常 JSON 响应，但即时分片显示取决于服务端流式能力及模型先输出语言字段。

## 本轮反馈排查与补充

- 从 Chrome Default 的扩展注册信息确认，OpenTransAI 实际加载目录为本项目 `dist/OpenTransAI`。只读取了匹配扩展的 ID 和路径元数据，没有读取 API Key。
- 修改前核对 source / dist / ZIP 中 content、core、service、stream、popup 文件，内容完全一致；文件中已为纯图标按钮和流式端口，没有设置 / 取消按钮。
- 直接读取用户正在测试的 Chrome 页面（Simon Willison 的 six-months-in-llms 文章）无障碍树，仍看到旧版“译 OpenTransAI / 查看原文 / 复制译文 / 设置”。据此确认实际页面运行的内容脚本与磁盘包不一致；旧版一次性翻译消息不会提供进度回调，走非流式请求。
- 增加保存成功后 `window.close()`；在浏览器预览中确认保存并关闭，模拟保存失败时表单和错误提示保留。预览仅用测试适配器记录 close 调用，没有改动真实 Key。
- 划词按钮补齐参考中的 32px 方形、1px 细边框及深色背景，保留原 SVG，按钮不显示文字。新增浏览器断言同时覆盖加载 / 成功 / 失败状态下只存在关闭、重译、复制三个操作；12 / 12 通过，控制台无错误或警告。
- 新增旧页面消息入口回归测试：先复现旧请求继续进入普通翻译路径，修复后直接提示刷新且不读取设置、不发起 AI 请求；30 / 30 Node 测试通过。
- 新代码已打包到 Chrome 当前注册目录。浏览器工具不能控制 `chrome://extensions` 内部页，已请用户点击重新加载；等待该操作后才能验证更新后的真实供应商流式输出与原生 popup 关闭。目前不能将本地模拟通过描述为真实 API 端到端通过。

## 原文折叠与长内容布局

- 调整前新增测试复现了原文默认展开、缺少字数、译文没有独立滚动区域。调整后在 1280×900、375×812、640×360 三种视口各完成 16 / 16 浏览器测试，无控制台错误或警告；Node 测试仍为 30 / 30，打包检查通过。
- 真实内容脚本：原文点击展开 / 折叠改变浮窗高度，短内容不会强制撑满上限。含 emoji 的字数不拆分 UTF-16 代理对。长原文和译文分别可滚动，外层内容区不滚动，两个滚动区隐藏滚动条；滚动前后语言栏和底部操作位置不变。
- 四角选区在片段增长及原文展开后均保留水平、垂直安全边距，面板高不超过视口 70%。浏览器缩放测试把已打开的长内容浮窗从 640×360 改为 375×600，可用宽度 360px，浮窗实测 336×420px、位置 (16, 8)，符合窄屏宽度和 70% 高度上限。
- 流式阅读位置测试：首段时译文跟随到底部，手动把原文滚至 40px、译文滚至 30px，下一片段完成后两个位置保持。另通过浏览器实际滚轮操作检查独立滚动，浮窗保持打开。
- 检查桌面展开态及窄屏截图：顶部、原文折叠栏、底部复制 / 重译均可见，无横向溢出、滚动条或布局遮挡。截图保存在任务可视化目录，未进入插件包。
- 本轮只改变内容布局及对应测试 / 说明；源码和 ZIP 已更新到既有加载目录，未操作用户 Chrome 的重新加载或真实 AI 服务。

## 思考开关与阶段提示（2026-09-11）

- 配置模型标签旁增加思考模式开关，默认关闭；只有 DeepSeek 官方 origin + OpenAI 兼容协议可操作。其他协议 / 域名不发送思考参数。启用、保存自动关闭、重新打开回显、关闭后再次保存回显、协议切换草稿恢复均通过浏览器验证。
- 400×600、375×600 配置窗口无横向溢出，保存按钮底部 578.5px。截图核对原有设置风格保留，开关及解释文案完整可见。
- 浏览器桌面与 375×600 窄屏各通过 18 / 18；等待时不预先显示思考，收到 PHASE 才切换，已用时更新，首段到达后收起，失败 / 关闭后计时停止。控制台无错误或警告。
- `npm test` 34 / 34：新增默认值 / 参数隔离、设置持久化、仅发送枚举阶段、重复事件去重、取消后禁止状态更新、原始推理文本不进入译文或端口消息等断言。
- 使用用户授权的 DeepSeek 配置和当前生产 `translate()` 做两次真实短句调用（单次样本，非性能保证；测试加 2048 token 输出上限）：关闭时 HTTP 200，请求 disabled，阶段 waiting → translating，首段 1010ms、完成 1054ms；开启时 HTTP 200，请求 enabled，阶段 waiting → thinking（1102ms）→ translating（1546ms），首段 1595ms、完成 1603ms。两次译文均通过格式及方向校验，均有 6 次增量更新。
- Key 仅用于指定官方 API 的授权调用，未写入项目文件或诊断结果；没有向浏览器预览填写真实 Key。真实 API 阶段和浏览器 UI 分别验证，用户 Chrome 原生扩展仍需重新加载。

## 浅色图标（2026-09-11）

- 新 SVG 与用户提供的 `ai-translate-light.svg` 字节一致；原暗色 SVG 及四种尺寸 PNG 的 SHA-256 与修改前一致。新浅色 PNG 独立保存在 `icons/light/`，两套图标都包含在安装包内。
- 工具栏、配置页、翻译框及划词按钮统一引用浅色资源；划词按钮维持 26px，采用配套浅色渐变、细边框及悬停效果。浏览器截图确认按钮和翻译框图标显示正确。
- `npm run package` 检查与打包通过；实际内容脚本的浏览器交互测试 18 / 18 通过，使用本地模拟响应。本轮未重新加载用户 Chrome 扩展。

## 协议切换保留当前表单（2026-09-11）

- 新增回归用例先复现“切换协议清空或替换了 API 地址”，修复后浏览器测试 19 / 19 通过。覆盖三种协议往返切换、地址 / 模型 / Key / 默认语言保留、修改后不恢复旧草稿、思考偏好恢复，以及保存关闭后重新加载的协议和字段回显。
- 在实际配置界面填入演示数据，从 OpenAI 兼容切至 Anthropic，地址、模型及演示 Key 均保留；Key 仍可正常切换显示与隐藏，控制台无错误或警告。未使用真实 Key 或调用外部 AI 服务。
- `npm test` 34 / 34、`npm run package` 和 `git diff --check` 通过；安装目录与 ZIP 已更新。仅修改本地代码与说明，未提交或推送，用户 Chrome 扩展需重新加载。
